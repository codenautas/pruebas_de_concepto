{
  "dependencies": {
    "fs-extra": "^0.18.3",
    "fs-promise": "^0.3.1",
    "best-promise": "0.2.3",
    "charset-detector": "0.0.2",
    "jschardet": "1.4.1"
  },
  "devDependencies": {
    "mocha": "~2.2.4",
    "expect.js": ">=0.3.1"
  },
  "scripts": {
    "test-jschardet": "mocha --globals confidence --reporter spec --bail --check-leaks .",
    "test": "env SKIP=jschardet,chardet-detector mocha --reporter spec --bail --check-leaks ."
  }
}